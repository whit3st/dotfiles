# Per-host overrides for um5606wa (ASUS Zenbook S16, pure AMD, 2880x1800@120).
# Dir name = DMI board_name, sanitized (see lib/detect.sh: dtf_machine).
# Only values that are NOT auto-detectable belong here — GPU/CPU/laptop/refresh
# are detected at runtime. Keep to plain KEY=value; sourced by scripts/dtf-display.sh.
DPI=144
