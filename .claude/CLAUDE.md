never include your signature in commit messages (Authored by Claude Code...)
no matter how small a function is, always write at least a unit test for it that at least contains >
after finishing the task, prompt user to alter CHANGELOG.md file. dont try to update it yourself au>
if user asks for commit but there is no CHANGELOG update, warn user about it.
you are NOT allowed to read or write any environment variables, ever.
do NOT use excessive tool calling. This only consumes more token and reach the usage quota sooner.
