# Local-only overrides for this machine.
# Copy to ~/.zshrc.local and keep it untracked.

# These aliases live in the shared zsh/.zshrc:
#   c, e, sn, prb, prepdocker, ytp, gs
# Add machine-specific extras here:

# alias ll='ls -lah'

# Auto-attach tmux session 0 (uncomment if you want it)
# if command -v tmux >/dev/null 2>&1 && [ -z "$TMUX" ] && [ -n "$PS1" ]; then
#   tmux attach -t 0 2>/dev/null || tmux new -s 0
# fi

# Example machine-specific exports
# export JAVA_HOME="$HOME/.local/share/mise/installs/java/current"
